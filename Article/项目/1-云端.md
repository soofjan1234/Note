## 客户端、服务端、云端及穿透端的通信流程

**情境** 

1. 核心问题是 NAT 会拦截无状态入站流量：内网设备只有先出站建立映射，外部回包才进得来
2. 所以会通过P2P穿透，那要进行穿透就得知道对方的公网地址
3. 而我负责的项目主要是信令服务器，在双方还没建立起直连通道之前，提供一个互相交换信息的媒介

**角色**

- 客户端：手机
- 服务端：nas
- 云端：我负责的项目，信令服务器
- 穿透端：公网上的"穿透服务器"

**链路**

1. 首先，都在同一局域网的时候，手机点击开启穿透，nas会请求云端领一些东西
    1. 穿透凭证，用于nas开启穿透
    2. 验证码，用于手机向云端获取信息，在明，可展示给用户
    3. 接头暗号，用于验证身份
2. 然后nas会拿着穿透凭证，去公网上的"穿透服务器"那里注册或创建会话
3. 手机现在在外面了，把验证码给云端，问他这人在哪，云端查到后就给他穿透测入口，并给他接头暗号
4. 最后就是完成身份校验与通道建立

## 穿透用到什么技术，如何通信

1. 用户层，我们是通过http over tcp访问本机 127.0.0.1:50080，穿透映射远程的50080端口到本机
2. 通道层：底层通道由穿透网络提供，优先使用 P2P 直连；直连失败时自动切到节点转发 / 中继转发
3. 内部实现上（由第三方 SDK 封装）会做：NAT 类型探测、 STUN、打洞什么的

## 怎么得到的公网 IP 地址和端口？

1. 在原理中，是通过stun服务器获取的，设备向stun服务器注册内网地址，服务器会记录公网地址
2. 在我们的实现中，客户端不需要得到公网地址，只需要知道对端是谁，然后调用穿透sdk封装了ConnectAdd即可


## P2P 失败云端怎么处理

云端只负责凭证的颁发，不具体处理这个，然而
1. 穿透SDK自带 Relay 中转作为 P2P 失败的兜底
2. 同时SDK也支持私网直连与局域网扫描，APP支持了该功能

## 为什么要加密

如果不加密，敏感字段会直接裸奔在链路中：

- NAS 设备 -> 云端：`给我发个访问码`
- 云端 -> NAS：`{accessID: "ABC123", accessVerify: "XYZ789", peerID: "peer001"}`

当前链路可以拆成两层：

1. **应用层加密**：设备侧先做 AES+RSA（混合加密）。
2. **传输层加密**：外层走 TLS（例如：`设备 -> [公网 TLS] -> Cloudflare -> http -> 云端`）。

若在 VPN 场景，常见链路是：`设备 -> [VPN 隧道] -> VPN 节点 -> [公网 TLS] -> Cloudflare`。

有些公司或特殊网络会要求在设备上安装其自有根证书（Root CA）。此时需注意：

- 一旦你信任了该根证书，VPN/代理节点理论上可以做“解密后再加密”（TLS 卸载 / 中间人代理）。
- 也就是说，**TLS 不再等同于“端到端只有你和服务端可见”**。

这时应用层 AES+RSA 的价值就体现出来了：

- 即便 TLS 在中间节点被解开，中间人看到的仍是应用层密文。
- 前提是：对端私钥和会话密钥管理得当，且加密实现没有漏洞。

## 为什么要引入消息队列

- **解耦**：HTTP 接口只负责业务（发凭证、查设备），「写操作记录/审计日志」不阻塞接口，通过发一条消息交给下游异步处理。
- **削峰**：请求突增时，写记录不会拖慢主流程，消息先堆积在队列里，消费者按能力消化。

## 为什么不用channel而是nsq

1. 消息不丢，nsq有超时重投、持久化机制
2. 削峰填谷，channel满了就阻塞；NSQ可以堆积在磁盘，慢慢消费
3. 后续可扩展，一个做日志，一个做统计