# MySQL 知识库重构计划

## 目标叙述
为了提高 MySQL 知识的系统性和深度，重写现有的笔记体系。
1. **剔除冗余**：移除“备份与恢复”相关内容，专注于核心原理。
2. **逻辑重组**：将零散的知识点按数据库底层逻辑重新编排。
3. **标准化**：为每篇文章制定统一的结构，便于复习和检索。

## 提议的更改

### 目录结构规划 (细化版)
为了降低阅读难度，将原有的大章拆分并映射重组为多个核心小节，确保涵盖原笔记（剔除备份与恢复）的所有概念点：

#### 1. 基础架构与引擎
- **1.1-MySQL系统架构.md**：
  - MySQL宏观层面的系统架构。
  - MyISAM 和 InnoDB 的本质区别（原 `1-存储引擎.md`）。
- **1.2-InnoDB核心组件与运行流程.md**：
  - 核心概念：内存结构 (Buffer Pool)、后台线程、Master Thread 的工作、Checkpoint 机制（原 `1-存储引擎.md`）。
- **1.3-InnoDB关键特性原理解析.md**：
  - 关键特性：Change Buffer (插入缓冲)、Doublewrite (两次写)、自适应哈希索引 (AHI)、异步IO (AIO)、刷新邻接页（原 `1-存储引擎.md`）。

#### 2. 存储逻辑与物理文件
- **2.1-物理文件结构.md**：
  - 逻辑存储结构（表空间、段、区、页）与 IOT（索引组织表）概念设计（原 `3-表.md`）。
  - 数据页的内部结构与组成（原 `3-表.md`）。
- **2.2-表与索引的行格式.md**：
  - 行记录格式的区别（Compact、Redundant、Dynamic、Compressed）（原 `3-表.md`）。
  - 溢出页机制带来的行溢出问题及处理（原 `3-表.md`）。

#### 3. 索引原理与性能优化
- **3.1-B+树索引原理.md**：
  - 为什么要使用B+树，B+树的插入与删除流转（原 `4-索引.md`）。
  - B+树索引的具体分类（聚簇索引与二级索引）（原 `4-索引.md`）。
  - 哈希与查找机制（原 `4-索引.md`）。
- **3.2-索引应用与优化.md**：
  - Cardinality值的生成（采样统计）与更新时机（原 `4-索引.md`）。
  - 为什么有索引比没索引快，以及可能导致索引失效的场景总结（原 `4-索引.md`）。

#### 4. 并发控制与锁
- **4.1-锁机制：类型与场景.md**：
  - Lock与Latch的区分，S锁、X锁、意向锁的基础定义（原 `5-锁.md`）。
  - 一致性锁定读、一致性非锁定读，以及自增锁、外键锁（原 `5-锁.md`）。
- **4.2-锁机制：加锁规则与死锁.md**：
  - 并发事务引发的问题（丢失更新、脏读、不可重复读、幻读）。
  - 锁的具体算法逻辑（Record Lock、Gap Lock、Next-Key Lock）（原 `5-锁.md`）。

#### 5. 数据一致性
- **5.1-事务模型与ACID.md**：
  - 事务的定义与其四大特性（ACID）。
  - 事务隔离级别的定义及对应的影响（原 `6-事务.md`）。
- **5.2-MVCC多版本并发控制.md**：
  - 什么是MVCC（读写冲突的文斗与武斗）（原 `8-mvcc.md`）。
  - UndoLog的数据前世今生、ReadView版本考勤快照（原 `8-mvcc.md`）。
  - RC与RR级别下Read View的运作表现，以及RR级别下对抗幻读的逻辑（原 `8-mvcc.md`）。

#### 6. 日志系统与故障恢复
- **6.1-Redo Log与崩溃恢复.md**：
  - Redo Log的物理结构、生命周期（原 `2-文件.md`）。
  - 为什么写日志快而写数据慢（WAL原理）、崩溃恢复的具体流程（原 `2-文件.md`）。
- **6.2-Undo Log与Purge机制.md**：
  - Undo Log 的空间管理与回滚段（原 `6-事务.md`）。
  - Purge 线程的运行机制与历史版本的垃圾清理逻辑（原 `6-事务.md`）。
- **6.3-Binlog与主从复制.md**：
  - Binlog基础概念与记录格式（原 `2-文件.md`）。
  - Redo Log与Binlog的对比差异（原 `2-文件.md`）。
- **6.4-核心串联：一条更新语句的完整流程.md**：
  - 结合两阶段提交的核心三步骤与各种宕机时刻的异常恢复逻辑（原 `2-文件.md`）。
  - 最终修改数据落地：结合 WAL、Doublewrite 的全景拼图（原 `9-其他.md`）。

---

## 验证计划
### 自动化检查
- 检查文件是否存在且编号正确。
- 检查是否包含所有的标准结构标题。

### 人工验证
- 邀请用户查看目录逻辑是否符合预期。
- 确认是否已移除“备份与恢复”的所有痕迹。
