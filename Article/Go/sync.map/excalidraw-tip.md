# Sync.Map 配图提示词（严格依据 Sync.Map.md 文案）

以下每段为**独立提示词**，可直接交给绘图 AI。内容必须仅来自 `Article/sync.map/Sync.Map.md`。

**使用约定（画到本地）**  
- 输出：用 Write 将图保存为 **Obsidian Excalidraw .md**，路径为 `Article/sync.map/Excalidraw/[下面各条指定的文件名].md`。  
- 风格：配色与版式参考同目录下已有图（如 `Sync.Map.基础概念.md`、`字典树.结构图.md`），遵循 excalidraw-diagram 技能的 Color Palette。  
- 文中引用：在 `Sync.Map.md` 中可用 `![[Excalidraw/文件名]]` 嵌入（不含 .md）。

---

## 1. 基础概念（已有）

已有图：`Sync.Map.基础概念.md`，无需重复绘制。

## 2. 字典树（已有Trie 结构图）

请绘制「字典树（Trie）」结构图，用于解释哈希树的思想来源。要求：
1. 说明文字：`Trie 通过前缀快速匹配`、`查找速度只与字符串长度有关，与字典大小无关`、`以空间换时间`。
2. 图中用 Apple、Cat 来说明，用 Apple 查找，横向的树。
3. 右侧加一个小注释框：`sync.Map 借鉴 Trie 思想`。  
**输出**：保存为 `Excalidraw/字典树.结构图.md`（若已存在可覆盖或另存为 `字典树.结构图.2.md`）。

## 3. sync.Map 哈希树（结构示意图）

请绘制「sync.Map 哈希树」示意图，严格体现文案中的规则：
1. 说明：`Key 的 hash 值被当作字符序列`。
2. 分层规则：`每 4 位为一层`。
3. 分支结构：`每个节点有 16 个子节点`；子节点用 4 位二进制标注（如 0000、0011、1111），对应该层 0～f 的 16 种取值。
4. 深度示意：`64 位 hash → 最大 16 层`（64÷4=16）。
5. 备注：**再小树太深、Load 会慢很多；再大节点太大、收益几乎可以忽略**。  
**输出**：保存为 `Excalidraw/sync.Map.哈希树.结构图.md`。

## 5. 添加、更新流程

请绘制「添加流程」流程图，步骤必须与文案一致：
1. 只显示一个indirect和entry，然后内部属性说清楚有什么
2. `锁住上一层的 indirect`，`再读一次槽和 i.dead`。
3. 若 `槽被改成 indirect` 或 `节点已死`：`解锁并从头重试`。
4. 持锁后分支：
   - `槽里已有 entry -> 更新`
   - `槽为空 -> 新增`
   - `槽里有 entry 但 key 不在链里 -> 扩展`
5. 注意：强调“持锁后”的三个分支。  
**输出**：保存为 `Excalidraw/sync.Map.添加流程.md`。

## 6. 扩展流程

请绘制「扩展流程」示意图，步骤与判断必须来自文案：
1. 触发条件：`同一槽已有 oldEntry，且新 key 的 hash 前缀与其相同但 key 不同`。
2. 方案 A（真冲突）：`两个 key 的完整 hash 相同 -> 另一个挂到 overflow`。
3. 方案 B（可区分）：`hash 在更低位不同 -> 用更低 4 位，把 old 与 new 分到不同槽`。
4. 过程以分支图呈现，清晰标注 `old` 与 `new`。  
**输出**：保存为 `Excalidraw/sync.Map.扩展流程.md`。

## 7. 最佳实践（要点图）

请绘制「最佳实践」要点图，内容严格来自文案：
1. 标题：`不要默认用 sync.Map`。
2. 说明：`多数代码应使用 map + 自己的锁（或其他同步），更安全且便于维护与 map 相关的其它变量`。
3. 列出“适合的两种场景”：
   - `一写多读：同一 key 只写一次，之后大量读取（只增不减的缓存/配置）`
   - `大量 goroutine 操作不同 key：key 集合几乎不重叠，如 per-goroutine 状态`
4. 用两列或列表清晰排版即可。  
**输出**：保存为 `Excalidraw/sync.Map.最佳实践.md`。
